# Base URL of your backend API (FastAPI/Flask wrapping the 2D CNN + 3D U-Net)
VITE_API_BASE_URL=http://localhost:8000

# true  -> UI works standalone with realistic fake predictions (no backend needed)
# false -> UI calls VITE_API_BASE_URL for real predictions
VITE_DEMO_MODE=true
